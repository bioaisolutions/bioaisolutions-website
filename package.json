{
  "name": "bioaisolutions-website",
  "description": "BioAI Solutions corporate website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build:assets && hugo server --bind 0.0.0.0 --port 13131",
    "build": "npm run build:assets && hugo --minify",
    "build:assets": "./scripts/copy-vendor.sh",
    "postinstall": "npm run build:assets"
  },
  "devDependencies": {
    "bootstrap": "^5.3.2",
    "boxicons": "^2.1.4",
    "jquery": "^3.7.1",
    "rellax": "^1.12.1",
    "swiper": "^10.3.1",
    "@lottiefiles/lottie-player": "^2.0.2"
  }
}
